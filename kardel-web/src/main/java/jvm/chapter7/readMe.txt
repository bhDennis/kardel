类与接口的初始化阶段不同之处：

    1.类中允许static代码块来输出初始化信息，接口中不允许

    2.对于接口，编译器会为接口生成<clinit>()类构造器，用于初始化接口中定义的成员变量

    3.接口与类有所区别的是有且仅有四种情况中的第三点：
      当一个类在初始化时要求其父类都初始化过了，但对于某个接口在初始化时，并不要求其父
      接口全部完成了初始化，只有真正使用到父接口的时候（如引用接口中定义的常量）才会初始化。

注：
    <clinit>是类构造器
    <init>是方法构造器