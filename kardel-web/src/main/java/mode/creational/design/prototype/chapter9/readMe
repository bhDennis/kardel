
p.11

原型模式：从一个对象再创建另外一个可定制的对象，而且不需要知道任何创建的细节。

浅拷贝与深拷贝：

    针对【浅拷贝】，被复制对象的所有变量都含有与原来的对象相同的值，而所有的对其他对象的引用都指向原来的对象。

    针对【深拷贝】，其实就是将要复制的对象的引用的对象都要复制一遍。深拷贝将引用对象的变量指向复制后的新对象。

    浅拷贝只能克隆基本类型的值，而深拷贝可以复制对象。

    深拷贝将引用对象的变量指向复制过的新对象，而不是原有的被引用的对象。