p.142

建造者模式(又名生成器模式)：

    将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示。

    使用场景：用于构造一个复杂的对象，这些对象内部构建间的构造顺序通常是稳定的，但对象内部的构建通常面临着复杂的变化。

    优点：在高内聚的前提下使得与外界的耦合大大降低。

    只需要表明要建造什么，可以使用相同的构建过程构建出不同的产品。
