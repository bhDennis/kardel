
p.289

【中介者模式】
    用一个中介对象来封装一系列的对象交互。
中介者使各个对象间不需要显示的相互引用，从而使其耦合松散，而且可以独立它们之间的交互。

优点：
    中介者的出现 减少了 各个实际对象之间的耦合，使得可以独立的复用和改变，各个实际对象之间的交互都会转移到中介者
身上来，从而提升到了一种看待问题的宏观角度。

缺点：
    将交互的复杂性全都转移到了中介者身上，这就使得中介者比任何一个实际对象复杂。

案例：如计算器中所有的控件交互都是由Form窗体这个中介者来完成。