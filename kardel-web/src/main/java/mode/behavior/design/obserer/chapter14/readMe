p.153

【观察者模式】（发布-订阅模式）
    让多个观察者同时监听某一个主题对象，这个主题对象的状态发生变化时，会通知到所有的观察者，
使他们能够自动更新自己。


包结构中的类分为两类：
    一类是不带后缀ext的，是我们平时经常用到的观察者模式写法，但是是有缺点的，AbstractSubject类如果不与AbstractObserver绑定就无法工作了。
    另一类是带ext的，可以解耦AbstractSubject类 和 AbstractObserver类