
p.297

【享元模式】

    运用共享技术支持大量细粒度的对象，可以避免大量非常相似类的开销。

    享元对象：里面包含两个状态，内部状态和外部状态。

    内部状态：不随环境改变而改变的共享部分。

    外部状态：随环境变而变，不能共享。

优点：减少对内存空间的使用，可以避免由于对象的数量过多导致的资源和性能的损耗。

缺点：我们需要维护一个记录系统已有的所有享元列表，其本身是需要消耗资源的。

示例中的 Website类，里面的Uer对象其实就是 外部状态，由外部传入而来。

另举一个例子：围棋、五子棋、跳棋。

    外部状态：方位坐标
    内部状态：颜色